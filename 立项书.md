# 项目 03 立项书 · AI-Assisted Cryptographic Code Security Auditor（AI 辅助密码代码安全审计）

> 立项状态：**旗舰项目 · 主体已建成并出数**（本立项书补正式立项，同步记录「已完成主体 + 收尾路线」）
> 立项日期：2026-09-11 ｜ CV 落点：`AI-Assisted Cryptographic Code Security Auditor`
> 建议 repo：`crypto-ai-auditor`（GitHub 公开）｜ 方法论关键词：**static-analysis × 领域适配 LLM × Agent 误报过滤**

---

## 项目基本信息

| 项 | 内容 |
|---|---|
| 项目名称 | AI-Assisted Cryptographic Code Security Auditor |
| 中文名称 | AI 辅助密码代码安全审计系统 |
| 项目定位 | 结合 Semgrep 自定义密码规则（召回）、QLoRA 微调的 Qwen3-4B（Confirm/Reject 分流）与 Agent 机制（误报过滤），对 Python 代码做漏洞检测、CWE 分类与风险分级，输出结构化安全报告 |
| 核心研究问题 | Can domain-adapted LLMs and tool-augmented agents improve the analysis and triage of cryptographic implementation risks?（含公共漏洞数据 → 密码领域的迁移性） |
| 一句话目标 | 证明「静态规则 × 微调 LLM × Agent」的混合流水线，比任一单模块检测更全、误报更低，并用真实数字量化 |
| 安全边界 | 防御性安全研究：检测密码误用、辅助审计、误报分流；不自动攻击真实系统、不生成恶意利用代码 |

---

## 1. 为什么做 / 要证明什么

传统密码代码审计高度依赖人工专家：静态规则能高精度命中已知误用（AES-ECB、硬编码密钥等），但误报高、不懂上下文；纯 LLM 能读上下文，但召回不稳。本项目要证明的是一条**「规则做召回、微调 LLM 做分流、Agent 做误报过滤」的可复现混合流水线**：

- **把 AI 用在安全上（而非只会跑现成工具）**：从自己写 10 条 Semgrep 密码规则，到 QLoRA 领域适配、再到 Agent 二次校验，全链路自研；
- **量化「领域微调是否真的有用」**：Base vs Fine-tuned 在检测 / CWE 分流 / 误报过滤上的提升用数字说话；
- **为 CV/产品叙事提供旗舰样例**：与「LLM 安全评测」（项目 01）形成「把 AI 当工具」+「对 AI 本身做评测」两条主线。

## 2. 目标与成功标准

**做一件事：** 一个他人能 `git clone` 后复跑「代码 → 静态分析 → LLM → 结构化安全报告」的 repo，README 里含五系统对比表。

### 核心指标（以真实测试集为准）
| 指标 | 口径 |
|---|---|
| Detection F1（通用切片） | PyCode-Vul 官方测试集（569 条）上，vulnerable 二分类的 F1 / Recall / Precision |
| CWE 分类准确率 | 通用切片 macro CWE Acc |
| 误报过滤（领域切片） | 密码候选 finding 经 Agent 分流后的 FPR（对比 Semgrep 全收基线） |
| 真实世界泛化 | 3~5 个真实 Python 密码项目上的 TP / FP / FN |

> **已取得结果（立项基线，即写入 CV 的实数）**：微调 Qwen3-4B 在 569 条 PyCode-Vul 留出集上 **F1 0.697**；在独立 18 例真实探针集上，目的导向推理把误报率从 **50% 降到 0%**，同时保留对已知漏洞样本的检出。

## 3. 范围与非目标

**范围内：** Python 代码的密码误用检测 + 通用漏洞检测（公共数据微调）、CWE 分类、严重度分级、误报过滤、结构化报告；10 条自研 Semgrep 密码规则。

**非目标（别陷进去）：**
- 不做 C/C++ 多语言支持（v1 仅 Python）；
- 不追求工业级规则精度（规则只负责「流程跑通 + 召回」，precision 交给 LLM/Agent）；
- 不追求覆盖所有 CWE（聚焦 9 个密码相关 CWE + 公共数据高频 CWE）；
- 不做攻击性利用 / 漏洞利用复现（守住防御边界）。

## 4. 方案设计

### 4.1 系统架构
```
用户提交 Python 代码
   ↓  Semgrep（10 条 CRYPTO 规则）→ Candidate Findings（领域切片存在；通用切片≈空）
   ↓  LLM Agent（Base / Fine-tuned Qwen3-4B）
   ↓     通用切片：函数级「是否漏洞 + CWE + 严重度」   （task: detect）
   ↓     领域切片：finding 级 Confirm/Reject 分流 + 修复建议（task: triage）
   ↓  结构化 JSON → Markdown 安全报告
```

### 4.2 静态规则（召回层）
10 条自研 CRYPTO 规则，每条 `semgrep --test` 自测通过：
`MD5` / `SHA-1` / `DES/3DES` / `RC4` / `AES-ECB` / `Predictable IV` / `Hard-coded Key` / `Weak Randomness` / `Weak Key Length` / `Insecure TLS`。
每条含 `rule_id / name / cwe / severity / pattern / description / recommendation / references`。

### 4.3 数据策略（四层 → 双切片）
| 层 | 来源 | 用途 |
|---|---|---|
| CWE 知识库 | MITRE CWE（重点 CWE-321/327/328/329/330/338/326/759/916） | 标签体系 + prompt/RAG |
| 规则库 | 自研 10 条 + 可选社区规则包 | 候选生成 |
| 公共漏洞数据 | **PyCode-Vul**（S-AIR-L/PyCode-Vul，cc-by-4.0，Zenodo DOI 19746552） | QLoRA 微调主料 + 通用切片评估 |
| 密码领域对照 | 自造 vulnerable/secure 对（经自有规则回测） | 领域切片 + Agent 分流评估 |

**实际划分（已落地）：** detect_train 2406 / detect_val 272 / detect_test 569 / triage_train 132 / domain_eval 29。
**防泄漏：** 按 repo 分组切分 + 规范化哈希去重 + 用官方 train/test 天然分集。

### 4.4 模型（领域适配层）
- 基础模型 **Qwen3-4B**；QLoRA 微调（**rank 16 / alpha 32**），在 **2,595 条**训练样本上从 base 重训。
- 训练格式：`task` 显式区分 detect（仅代码 → 漏洞判断）/ triage（代码 + finding → Confirm/Reject）。
- 关键教训（如实记录）：增量微调出现**灾难性遗忘**，改为从 base 重训 + 重设计数据划分与评测流程后恢复。

### 4.5 Agent 与误报过滤
- **v0（已落地）**：单步推理 + fixture guard（test/夹具代码不自动 Confirm，压制测试/夹具代码告警）。
- **P4（待办）**：ReAct 工具环（semgrep / AST / rule_db / cwe_db / patch）+ 反谄媚 Prompt（要求证据、倾向 Reject 测试/mock/非敏感）。

### 4.6 评估协议（五系统 × 双切片）
| 系统 | 通用 Detection F1 | 通用 CWE Acc | 领域 Recall / FPR |
|---|---|---|---|
| ① Static only | | | |
| ② Base LLM | | | |
| ③ Static + Base | | | |
| ④ Static + Fine-tuned | | | |
| ⑤ Full Agent | | | |

## 5. 环境与资源
- 本地：Python 3.10+、Semgrep、固定 seed / 解码参数；无 GPU，推理与微调走云 API（阿里云百炼 / 魔搭）。
- 微调记录（配置 / loss / 成本 / 耗时）入技术报告；API key 放 `.env` 不入库。

## 6. 仓库结构
```
crypto-ai-auditor/
├── README.md            # EN：方法 + 五系统结果表 + 复跑
├── rules/               # 10 条 CRYPTO 规则（semgrep --test 通过）
├── analyzer/            # semgrep_runner / parser / extractor
├── agent/               # agent.py（v0）+ prompts + verifier
├── model/               # inference.py（cloud/mock）+ finetune
├── evaluation/          # benchmark.py + metrics + configs
├── data/                # raw / processed / splits（代码大文件不入 git）
├── scripts/             # import_public_dataset / prepare_sft_data / validate_dataset
├── reports/  docs/      # 报告 + dataset.md + architecture.md
└── requirements.txt
```

## 7. 时间表与当前进展

| 阶段 | 状态 | 交付 |
|---|---|---|
| P0–P1 数据底座 + 规则 | ✅ 已完成 | repo、10 条规则、PyCode-Vul 导入与防泄漏划分、schema |
| P2–P3 基线 + 云微调 | ✅ 已完成 | Base vs FT；FT 在 569 条测试集 F1 0.697 |
| P4 Agent 工具环 | 🔄 进行中 | ReAct 工具环 + 反谄媚 + 领域 FPR 对比 |
| P5 真实项目验证 | ⏳ 待办 | 3~5 个真实 Python 密码项目 TP/FP/FN |
| P6 打包上线 | ⏳ 待办 | README / Demo / 技术报告 / 数据卡 → 推 GitHub |

## 8. 风险与对策

| 风险 | 对策 |
|---|---|
| 公共数据标签噪声 | 用官方划分 + 5% 抽检人工核对 |
| detect/triage 双格式混训让 4B 学乱 | prompt 显式 task 标记；必要时分两轮微调 |
| Agent 谄媚（无证据也 Confirm） | 反谄媚 Prompt + 已知 FP 测试集 |
| 防泄漏不到位 | repo 分组 + 哈希 + 官方 split |
| 云平台缺模型 / 成本超支 | 兜底链：换平台 → 同族模型 → 租 GPU，设成本上限 |

## 9. 完成定义（DoD）→ CV 回填

- [x] 10 条规则 + 混合流水线跑通（代码 → 报告）
- [x] FT 模型检测 F1 0.697（569 条 PyCode-Vul 留出集）
- [x] 18 例真实探针 FPR 50% → 0%
- [ ] P4 Agent 工具环 + 领域切片 FPR 对比
- [ ] 3~5 真实项目端到端报告
- [ ] repo 公开 + README（EN）五系统表 + `make all` 可复跑
