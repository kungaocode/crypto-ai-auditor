# Round-4 real-project validation (held-out). All GT is safe:
#   detect -> 7 secure functions (vulnerable=false)
#   domain -> 35 Semgrep hits on crypto libraries (verdict=Reject)
# Meaningful metrics: domain fpr -> 0, domain accuracy -> 1.0, detect f1/accuracy.
# See data/round4/ROUND4_GUIDE.md §五.

model:
  kind: cloud
  model_id: qwen3-4b  # set via env LLM_MODEL_ID to the fine-tuned model
  # api_key:          # env LLM_API_KEY

static:
  rules: rules/

splits:
  detect: data/round4/real_validation/detect.jsonl
  domain: data/round4/real_validation/triage.jsonl

systems:
  - llm
